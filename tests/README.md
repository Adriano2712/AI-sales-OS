# tests (cross-app e2e)

Empty in Phase 0 — there's no product flow yet to exercise end-to-end. Starts
filling in once Campaigns (Fase 1) and Discovery (Fase 2) exist, following the
flow in spec section 68: Campaign → Discovery → Company → Analysis →
Opportunity → Dashboard.

Backend unit/integration tests live in `apps/api/tests/` instead, next to the
code they test.
